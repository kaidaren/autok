# 每次更新代码推送到 GitHub（默认工作流）

本项目的默认远程仓库已设置为用户自己的 GitHub：

- **fork 仓库地址（用户仓库）**：`https://github.com/kaidaren/autok`
- **上游仓库地址（社区仓库）**：`https://github.com/autox-community/AutoX_aiselp`

- `origin`: `https://github.com/kaidaren/autok.git`
- `upstream`: `https://github.com/autox-community/AutoX_aiselp.git`（仅用于拉取上游，不作为默认推送）

## 每次更新代码后的标准操作（必须）

在仓库根目录执行：

0) **先写更新日志（强制，不可跳过）**

- 先更新仓库根目录 `CHANGELOG.md` 的 `## [Unreleased]` 小节，再允许提交和推送。
- 日志必须是“可恢复、可回滚、可追踪”的详细记录，至少包含：
  - 变更背景/问题现象（用户视角）
  - 根因（代码/机制层）
  - 修改文件清单
  - 行为变化（修复了什么、不会影响什么）
  - 恢复/回滚指引（恢复到哪个 commit、或撤销哪些文件）
  - 验证步骤（如何复测）

1) 查看变更

- `git status`

2) 暂存全部变更（包含新增文件）

- `git add -A`

3) 提交（写清楚改动目的，且与 CHANGELOG 对齐）

- `git commit -m "feat/fix/chore: <简要说明>"`

4) 推送到 GitHub（默认推送到 `origin`）

- `git push`

## 分支与跟踪约定

- 当前主要工作分支为 `setup-v7`
- 本地 `setup-v7` 跟踪 `origin/setup-v7`，因此 `git push` 无需额外参数

## 常见注意事项

- 若推送出现大文件警告（>50MB），优先评估是否需要 Git LFS，或移除不必要的大资源文件再提交。
- 如果需要同步上游更新，使用：
  - `git fetch upstream`
  - `git merge upstream/setup-v7`（或按需要选择 rebase）
- 若未更新 `CHANGELOG.md`，禁止执行推送。
- 每次推送后需要在提交信息或工作记录中标注：
  - 本次对应 `CHANGELOG.md` 的条目标题
  - 对应 commit hash
  - 关键恢复锚点（如“回滚到 xxx commit”或“仅还原 xxx 文件”）

## CHANGELOG 书写模板（强制）

每次在 `## [Unreleased]` 下新增一个时间戳小节，建议格式：

- `### YYYY-MM-DD HH:mm <短标题>`
- `- 背景：...`
- `- 根因：...`
- `- 修改：<文件1>、<文件2>...`
- `- 影响：...`
- `- 恢复：...`
- `- 验证：...`
